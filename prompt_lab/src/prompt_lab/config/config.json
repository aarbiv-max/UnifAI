{
  "input_dataset_repo": "${INPUT_DATASET_REPO}",
  "input_dataset_file_name": "${INPUT_DATASET_FILE_NAME}",
  "output_dataset_repo": "${OUTPUT_DATASET_REPO}",
  "output_dataset_file_name": "${OUTPUT_DATASET_FILE_NAME}",
  "tokenizer_path": "${PROMPT_LAB_MODEL_HF_ID}",
  "model_name": "${PROMPT_LAB_MODEL_HF_ID}",
  "model_api_url": "${MODEL_API_URL}",
  "model_max_generation_length": "${PROMPT_LAB_MAX_GENERATION_LENGTH}",
  "model_max_context_length": "${PROMPT_LAB_MAX_CONTEXT_LENGTH}",
  "batch_size": "${PROMPT_LAB_BATCH_SIZE}",
  "max_retry": "${MAX_RETRY}",
  "orbiter_queue_target_size": "${QUEUE_TARGET_SIZE}",
  "orbiter_queue_name": "prompts_process_queue",
  "orbiter_task_name": "prompt_lab.celery_app.tasks.orbiter",
  "reviewer_queue_name": "reviewer_queue",
  "reviewed_prompts_queue_name": "reviewed_queue",
  "reviewer_task_name": "celery_app.tasks.fetch_prompt_lab_generated_objects",
  "reviewer": "${REVIEWER}",
  "landing_task_name": "prompt_lab.celery_app.tasks.landing",
  "rabbitmq_port": "${RABBITMQ_PORT}",
  "rabbitmq_ip": "${RABBITMQ_IP}",
  "mongodb_port": "${MONGODB_PORT}",
  "mongodb_ip": "${MONGODB_IP}",
  "templates_path": "${TEMPLATE_NAME}",
  "templates_dir_path": "projects_templates/",
  "templates_agent": "${TEMPLATE_AGENT}",
  "templates_type": "${TEMPLATE_TYPE}",
  "templates_project_context": "${TEMPLATE_PROJECT_CONTEXT}",
  "project_id": "${PROJECT_ID}",
  "project_repo": "${PROJECT_REPO}",
  "process_id": "${PROCESS_ID}"
}
