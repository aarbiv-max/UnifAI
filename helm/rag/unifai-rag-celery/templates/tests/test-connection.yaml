apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "unifai-rag-celery.fullname" . }}-test-connection"
  labels:
    {{- include "unifai-rag-celery.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "unifai-rag-celery.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
