apiVersion: v1
kind: Service
metadata:
  name: {{ include "multiagent-temporal-worker.fullname" . }}
  labels:
    {{- include "multiagent-temporal-worker.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "multiagent-temporal-worker.selectorLabels" . | nindent 4 }}