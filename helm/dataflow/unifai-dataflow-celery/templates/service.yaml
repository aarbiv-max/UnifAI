{{- if .Values.service.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "unifai-dataflow-celery.fullname" . }}
  labels:
    {{- include "unifai-dataflow-celery.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "unifai-dataflow-celery.selectorLabels" . | nindent 4 }}
{{- end }}