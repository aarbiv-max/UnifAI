🎉 vLLM {{ .Values.vllm.model }} has been deployed successfully!

{{- if .Values.route.enabled }}

🌐 EXTERNAL ACCESS (OpenShift Route):
{{- if .Values.route.host }}
  Route URL: http://{{ .Values.route.host }}
{{- else if .Values.route.clusterDomain }}
  Route URL: http://{{ include "vllm-qwen3.fullname" . }}-{{ .Values.namespace | default .Release.Namespace }}.{{ .Values.route.clusterDomain }}
{{- else }}
  Route URL: Auto-generated by OpenShift
  Get the route URL: oc get route {{ include "vllm-qwen3.fullname" . }} -n {{ .Values.namespace | default .Release.Namespace }} -o jsonpath='{.spec.host}'
{{- end }}

  Test the API:
  curl -X GET "http://$(oc get route {{ include "vllm-qwen3.fullname" . }} -n {{ .Values.namespace | default .Release.Namespace }} -o jsonpath='{.spec.host}')/health"

{{- end }}

📋 ACCESS OPTIONS:

1. {{- if .Values.route.enabled }} OpenShift Route (Recommended):
{{- if .Values.route.host }}
   curl http://{{ .Values.route.host }}/health
{{- else }}
   export ROUTE_URL=http://$(oc get route {{ include "vllm-qwen3.fullname" . }} -n {{ .Values.namespace | default .Release.Namespace }} -o jsonpath='{.spec.host}')
   curl $ROUTE_URL/health
{{- end }}

2. {{- else }}1. {{- end }} Port Forward (Local Access):
   export POD_NAME=$(kubectl get pods --namespace {{ .Values.namespace | default .Release.Namespace }} -l "{{ include "vllm-qwen3.selectorLabels" . }}" -o jsonpath="{.items[0].metadata.name}")
   kubectl --namespace {{ .Values.namespace | default .Release.Namespace }} port-forward $POD_NAME 8080:8000
   # Then access: http://localhost:8080

{{- if eq .Values.service.type "LoadBalancer" }}

3. LoadBalancer (if available):
   NOTE: It may take a few minutes for the LoadBalancer IP to be available.
   export SERVICE_IP=$(kubectl get svc --namespace {{ .Values.namespace | default .Release.Namespace }} {{ include "vllm-qwen3.fullname" . }} --template "{{"{{ range (index .status.loadBalancer.ingress 0) }}{{.}}{{ end }}"}}")
   curl http://$SERVICE_IP:{{ .Values.service.port }}/health
{{- end }}

⏱️  IMPORTANT: Model Loading Time
The {{ .Values.vllm.model }} model may take 10-20 minutes to load initially.
Monitor the pod logs: kubectl logs --namespace {{ .Values.namespace | default .Release.Namespace }} -l "{{ include "vllm-qwen3.selectorLabels" . }}" -f

🔍 STATUS CHECKS:

• Pod Status:
  kubectl get pods --namespace {{ .Values.namespace | default .Release.Namespace }} -l "{{ include "vllm-qwen3.selectorLabels" . }}"

• Deployment Status:
  kubectl get deployment --namespace {{ .Values.namespace | default .Release.Namespace }} {{ include "vllm-qwen3.fullname" . }}

{{- if .Values.route.enabled }}
• Route Status:
  oc get route {{ include "vllm-qwen3.fullname" . }} -n {{ .Values.namespace | default .Release.Namespace }}
{{- end }}

🧪 API TESTING:

• Health Check:
{{- if .Values.route.enabled }}
  curl http://$(oc get route {{ include "vllm-qwen3.fullname" . }} -n {{ .Values.namespace | default .Release.Namespace }} -o jsonpath='{.spec.host}')/health
{{- else }}
  curl http://localhost:8080/health  # (via port-forward)
{{- end }}

• List Models:
{{- if .Values.route.enabled }}
  curl http://$(oc get route {{ include "vllm-qwen3.fullname" . }} -n {{ .Values.namespace | default .Release.Namespace }} -o jsonpath='{.spec.host}')/v1/models
{{- else }}
  curl http://localhost:8080/v1/models  # (via port-forward)
{{- end }}

• Generate Text:
{{- if .Values.route.enabled }}
  curl -X POST http://$(oc get route {{ include "vllm-qwen3.fullname" . }} -n {{ .Values.namespace | default .Release.Namespace }} -o jsonpath='{.spec.host}')/v1/completions \
{{- else }}
  curl -X POST http://localhost:8080/v1/completions \
{{- end }}
    -H "Content-Type: application/json" \
    -d '{
      "model": "{{ .Values.vllm.model }}",
      "prompt": "Hello! Tell me about artificial intelligence.",
      "max_tokens": 100,
      "temperature": 0.7
    }'

⚙️  CONFIGURATION:
- Model: {{ .Values.vllm.model }}
- GPU Count: {{ .Values.gpu.count }}
- GPU Memory Utilization: {{ .Values.vllm.gpuMemoryUtilization }}
- Max Model Length: {{ .Values.vllm.maxModelLen }}
- Quantization: {{ .Values.vllm.quantization }}
{{- if .Values.healthProbes.readinessProbe.enabled }}
- Readiness Probe: {{ .Values.healthProbes.readinessProbe.initialDelaySeconds }}s delay
{{- end }}
{{- if .Values.healthProbes.livenessProbe.enabled }}
- Liveness Probe: {{ .Values.healthProbes.livenessProbe.initialDelaySeconds }}s delay
{{- end }}

📚 Need help? Check the logs for any issues:
kubectl logs --namespace {{ .Values.namespace | default .Release.Namespace }} -l "{{ include "vllm-qwen3.selectorLabels" . }}" --tail=50 