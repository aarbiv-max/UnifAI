apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "qdrant.fullname" . }}-test-connection"
  labels:
    {{- include "qdrant.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "qdrant.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
