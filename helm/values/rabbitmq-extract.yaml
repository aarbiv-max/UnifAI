# RabbitMQ extracted values - manually synced from shared-resource-values.yaml rabbitmq section
# Keep this in sync by running: yq '.rabbitmq' shared-resource-values.yaml > rabbitmq-extract.yaml

image:
  registry: images.paas.redhat.com/genie
  repository: rabbitmq
  pullPolicy: IfNotPresent
  tag: 4.0.5-debian-12-r0

service:
  type: LoadBalancer
