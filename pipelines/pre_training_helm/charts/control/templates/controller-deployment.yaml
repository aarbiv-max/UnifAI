apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-{{ .Chart.Name }}
  namespace: {{ .Values.global.namespace }}
  labels:
    app: {{ .Release.Name }}-{{ .Chart.Name }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: {{ .Release.Name }}-{{ .Chart.Name }}
  template:
    metadata:
      labels:
        app: {{ .Release.Name }}-{{ .Chart.Name }}
    spec:
      
      initContainers:
      - name: wait-for-services
        image: images.paas.redhat.com/genie/ubi8:latest
        env:
        - name: RELEASE_NAME
          value: {{.Release.Name| upper}}
        - name: CHART_NAME
          value: {{ .Chart.Name }}

        command: 
        - sh
        - -c
        - "/mnt/config/wait-for-services.sh"

        volumeMounts:
          - name: {{ .Release.Name }}-wait-for-services
            mountPath: /mnt/config
      
      volumes:

      - name: {{ .Release.Name }}-wait-for-services
        configMap:
          name: {{ .Release.Name }}-wait-for-services
          defaultMode: 0755

      containers:
      - name: {{ .Chart.Name }}-landing
        image: {{ .Values.landing.image.repository }}:{{ .Values.landing.image.tag }}
        imagePullPolicy: {{ .Values.landing.image.pullPolicy }}
  
        args: [
          {{- range .Values.landing.args }}
          '{{ . }}',
          {{- end }}
          ]


        #command: [ "/bin/bash", "-c", "--" ]
        #args: [ "while true; do sleep 3000000; done;" ]

        env:
        {{- range $key, $value := .Values.global.promptlab_env }}
        - name: "{{ $key }}"
          value: "{{ $value }}"
        {{- end }}
        
        - name: HF_HOME
          value: "/tmp/.cache"  
        - name: HF_TOKEN
          valueFrom:
            secretKeyRef:
              name: "{{.Values.global.hf_token}}"
              key: token

        - name: RABBITMQ_IP
          value: $({{ .Release.Name | upper }}_RABBITMQ_SVC_SERVICE_HOST)
        - name: RABBITMQ_PORT
          value: $({{ .Release.Name | upper }}_RABBITMQ_SVC_SERVICE_PORT)

        {{- if .Values.global.connection }}
        - name: MONGODB_IP
          value: {{ .Values.global.connection }}
        - name: MONGODB_PORT
          value: "27017"
        {{- else }}
        - name: MONGODB_IP
          value: $({{ .Release.Name | upper }}_MONGODB_SVC_SERVICE_HOST)
        - name: MONGODB_PORT
          value: $({{ .Release.Name | upper }}_MONGODB_SVC_SERVICE_PORT)
        {{- end }}

        {{- if .Values.global.process_id }}
        - name: PROCESS_ID
          value: {{ .Values.global.process_id }}
        {{- end }}

        - name: CELERY_WORKER_CONCURRENCY
          value: "{{.Values.landing.celery_worker_concurrency}}"
        - name: CELERY_WORKER_PREFETCH_COUNT
          value: "{{.Values.landing.celery_worker_prefetch_count}}"

        resources:
          requests:
            memory: {{.Values.resources.requests.memory}}
            cpu: {{.Values.resources.requests.cpu}}
          limits:
            memory: {{.Values.resources.limits.memory}}
            cpu: {{.Values.resources.limits.cpu}}          

      - name: {{ .Chart.Name }}-launchpad
        image: {{ .Values.launchpad.image.repository }}:{{ .Values.launchpad.image.tag }}
        imagePullPolicy: {{ .Values.launchpad.image.pullPolicy }}
        command: 
        - bash
        - -c
        - |
          promptlab launchpad
          echo "==================================================================================="
          echo "  Process completed successfully!                                                  "
          echo "  Make sure a JSON file uploaded to your repository before removing the deployment "
          echo "==================================================================================="
          sleep infinity

        #command: [ "/bin/bash", "-c", "--" ]
        #args: [ "while true; do sleep 3000000; done;" ]

        env:
        - name: HF_HOME
          value: "/tmp/.cache"     
        - name: HF_TOKEN
          valueFrom:
            secretKeyRef:
              name: "{{.Values.global.hf_token}}"
              key: token

        {{- range $key, $value := .Values.global.promptlab_env }}
        - name: "{{ $key }}"
          value: "{{ $value }}"
        {{- end }}

        - name: RABBITMQ_IP
          value: $({{ .Release.Name | upper }}_RABBITMQ_SVC_SERVICE_HOST)
        - name: RABBITMQ_PORT
          value: $({{ .Release.Name | upper }}_RABBITMQ_SVC_SERVICE_PORT)

        {{- if .Values.global.connection }}
        - name: MONGODB_IP
          value: {{ .Values.global.connection }}
        - name: MONGODB_PORT
          value: "27017"
        {{- else }}
        - name: MONGODB_IP
          value: $({{ .Release.Name | upper }}_MONGODB_SVC_SERVICE_HOST)
        - name: MONGODB_PORT
          value: $({{ .Release.Name | upper }}_MONGODB_SVC_SERVICE_PORT)
        {{- end }}
        
        {{- if .Values.global.process_id }}
        - name: PROCESS_ID
          value: {{ .Values.global.process_id }}
        {{- end }}

        resources:
          requests:
            memory: {{.Values.resources.requests.memory}}
            cpu: {{.Values.resources.requests.cpu}}
          limits:
            memory: {{.Values.resources.limits.memory}}
            cpu: {{.Values.resources.limits.cpu}}  

      securityContext:
        seccompProfile:
          type: RuntimeDefault