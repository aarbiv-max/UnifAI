apiVersion: v1
kind: Service
metadata:
  name: genie-training-svc
  labels:
    app: {{ .Values.Global.app }}
spec:
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: {{ .Values.Global.app }}
