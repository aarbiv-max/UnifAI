apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: unifai-rabbitmq
  namespace: tag-ai--runtime-int
  annotations:
    argocd.argoproj.io/sync-wave: "2"
  labels:
    app.kubernetes.io/name: unifai-rabbitmq
    app.kubernetes.io/component: message-queue
    app.kubernetes.io/part-of: unifai-shared-resources
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: https://gitlab.cee.redhat.com/ai_tools/unifai.git
    targetRevision: GENIE-727/story/gitops-unifai #HEAD
    path: helm/shared-resources/rabbitmq
    helm:
      valueFiles:
        - ../../values/rabbitmq-extract.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: tag-ai--runtime-int
  syncPolicy:
    automated:
      prune: false  # Don't auto-delete message queue
      selfHeal: true
    syncOptions:
      - CreateNamespace=false
      - RespectIgnoreDifferences=true
    retry:
      limit: 5
      backoff:
        duration: 15s
        factor: 2
        maxDuration: 5m
  ignoreDifferences:
    - group: ""
      kind: PersistentVolumeClaim
      jsonPointers:
        - /spec/resources/requests/storage
        - /metadata/finalizers  # Ignore finalizers - Kubernetes manages these automatically
    - group: apps
      kind: StatefulSet
      jsonPointers:
        - /spec/replicas
