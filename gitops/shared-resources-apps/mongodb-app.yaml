apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: unifai-mongodb
  namespace: tag-ai--runtime-int
  annotations:
    argocd.argoproj.io/sync-wave: "2"
  labels:
    app.kubernetes.io/name: unifai-mongodb
    app.kubernetes.io/component: database
    app.kubernetes.io/part-of: unifai-shared-resources
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: https://gitlab.cee.redhat.com/ai_tools/unifai.git
    targetRevision: GENIE-727/story/gitops-unifai #HEAD
    path: helm/shared-resources/mongodb
    helm:
      valueFiles:
        - ../../values/mongodb-extract.yaml         # Complete MongoDB config (derived from shared-resource-values.yaml)
  destination:
    server: https://kubernetes.default.svc
    namespace: tag-ai--runtime-int
  syncPolicy:
    automated:
      prune: false  # Never auto-delete databases
      selfHeal: true
    syncOptions:
      - CreateNamespace=false
      - RespectIgnoreDifferences=true
    retry:
      limit: 5
      backoff:
        duration: 15s
        factor: 2
        maxDuration: 5m
  ignoreDifferences:
    - group: ""
      kind: PersistentVolumeClaim
      jsonPointers:
        - /spec/resources/requests/storage
    - group: apps
      kind: StatefulSet
      jsonPointers:
        - /spec/replicas
